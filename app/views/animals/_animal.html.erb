<table>
  <tr>
    <th>Nome</th>
    <th>Tipo</th>
    <th>Custo mensal</th>
    <th></th>
    <th></th>
  </tr>

  <% @person.animals.each do |animal| %>
    <tr>
      <td><%= animal.nome %></td>
      <td><%= animal.tipo %></td>
      <td><%= animal.custo_mensal %></td>
      <td><%= link_to 'Editar', edit_person_animal_path(animal) %></td>
      <td><%= link_to 'Apagar', person_animal_path(animal), method: :delete, data: { confirm: 'Tem certeza?' } %></td>
    </tr>
  <% end %>
</table>
